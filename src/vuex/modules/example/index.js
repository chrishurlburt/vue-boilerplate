/* eslint no-shadow:0*/
import * as types from './mutation-types'
import * as actions from './actions'
import * as getters from './getters'

const state = {
  // example: null,
}

const mutations = {
  // [types.EXAMPLE_MUTATION](state) {
  //   state.example = someVal
  // },
}

export default {
  state,
  mutations,
  actions,
  getters,
}
